openapi: 3.1.0
info:
  title: tyrs-hand Management API
  version: 0.1.0
  license:
    name: MIT
    identifier: MIT
servers:
  - url: /api/v1
tags:
  - name: Auth
  - name: GitHub
  - name: Resources
  - name: Settings
  - name: Discord
  - name: Workspaces
  - name: Workers
  - name: SSH
  - name: Worker Protocol
  - name: Client Protocol
paths:
  /webhooks/github:
    post:
      operationId: receiveGitHubWebhook
      tags: [GitHub]
      servers: [{ url: / }]
      security: []
      parameters:
        - { name: X-Hub-Signature-256, in: header, required: true, schema: { type: string } }
        - { name: X-GitHub-Delivery, in: header, required: true, schema: { type: string } }
        - { name: X-GitHub-Event, in: header, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "202":
          description: 已持久化
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        default: { $ref: "#/components/responses/Problem" }
  /internal/v1/tools/call:
    post:
      operationId: callInternalTool
      tags: [Resources]
      servers: [{ url: / }]
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "200":
          description: Tool Call 结果
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        default: { $ref: "#/components/responses/Problem" }
  /setup/status:
    get:
      operationId: getSetupStatus
      tags: [Auth]
      security: []
      responses:
        "200":
          description: Setup 状态
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SetupStatus" }
  /setup/admin:
    post:
      operationId: setupAdministrator
      tags: [Auth]
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SetupRequest" }
      responses:
        "201":
          description: 管理员与恢复资料
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SetupResult" }
        default: { $ref: "#/components/responses/Problem" }
  /auth/login:
    post:
      operationId: login
      tags: [Auth]
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "200":
          description: 登录会话
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LoginResult" }
        default: { $ref: "#/components/responses/Problem" }
  /auth/me:
    get:
      operationId: getCurrentAdministrator
      tags: [Auth]
      responses:
        "200":
          description: 当前管理员
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Administrator" }
        default: { $ref: "#/components/responses/Problem" }
  /auth/logout:
    post:
      operationId: logout
      tags: [Auth]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "204": { description: 已退出 }
        default: { $ref: "#/components/responses/Problem" }
  /github/app:
    get:
      operationId: getGitHubApp
      tags: [GitHub]
      responses:
        "200":
          description: GitHub App 状态
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GitHubAppStatus" }
    put:
      operationId: putGitHubApp
      tags: [GitHub]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GitHubAppInput" }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /github/app/manifest:
    get:
      operationId: getGitHubAppManifest
      tags: [GitHub]
      responses:
        "200":
          description: Manifest 表单参数
          content:
            application/json:
              schema:
                type: object
                required: [url, manifest]
                properties:
                  url: { type: string, format: uri }
                  manifest: { type: string }
  /github/app/manifest/callback:
    get:
      operationId: githubAppManifestCallback
      tags: [GitHub]
      parameters:
        - name: code
          in: query
          required: true
          schema: { type: string }
      responses:
        "302": { description: 返回 GitHub 设置页 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/github-agent-instructions:
    get:
      operationId: getGitHubAgentInstructions
      tags: [Settings]
      responses:
        "200":
          description: 仅注入 GitHub Work Item 的 developer instructions
          content: { application/json: { schema: { $ref: "#/components/schemas/GitHubAgentInstructions" } } }
        default: { $ref: "#/components/responses/Problem" }
    put:
      operationId: putGitHubAgentInstructions
      tags: [Settings]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/GitHubAgentInstructions" } } }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/github-agent:
    get:
      operationId: listGitHubAgentSettings
      tags: [Settings]
      responses:
        "200":
          description: GitHub Agent Profile 与仓库参数覆盖
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GitHubAgentSettingsList" }
        default: { $ref: "#/components/responses/Problem" }
  /settings/github-agent/repositories/{id}:
    put:
      operationId: putRepositoryGitHubAgentSettings
      tags: [Settings]
      parameters:
        - { $ref: "#/components/parameters/CSRFToken" }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/GitHubAgentPreferences" } } }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/discord:
    get:
      operationId: getDiscordSettings
      tags: [Discord]
      responses:
        "200":
          description: Discord 设置
          content: { application/json: { schema: { $ref: "#/components/schemas/DiscordSettings" } } }
        default: { $ref: "#/components/responses/Problem" }
    put:
      operationId: putDiscordSettings
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/DiscordSettingsInput" } } }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /discord/status:
    get:
      operationId: getDiscordStatus
      tags: [Discord]
      responses:
        "200":
          description: Discord 运行状态
          content: { application/json: { schema: { $ref: "#/components/schemas/DiscordStatus" } } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/initializations/preflight:
    post:
      operationId: preflightDiscordInitialization
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/DiscordInitializationInput" } } }
      responses:
        "200":
          description: 零修改预检结果
          content: { application/json: { schema: { $ref: "#/components/schemas/DiscordPreflight" } } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/initializations:
    post:
      operationId: createDiscordInitialization
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/DiscordInitializationInput" } } }
      responses:
        "202":
          description: 初始化已排队
          content: { application/json: { schema: { $ref: "#/components/schemas/IDResource" } } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/initializations/{id}:
    get:
      operationId: getDiscordInitialization
      tags: [Discord]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: 初始化状态
          content: { application/json: { schema: { $ref: "#/components/schemas/DiscordInitialization" } } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/members:
    get:
      operationId: listDiscordMembers
      tags: [Discord]
      responses:
        "200":
          description: Discord 成员
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/DiscordMember" } }
        default: { $ref: "#/components/responses/Problem" }
  /workspaces:
    get:
      operationId: listWorkspaces
      tags: [Workspaces]
      responses:
        "200":
          description: Worker 与逻辑 Workspace 绑定
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WorkspaceList" }
        default: { $ref: "#/components/responses/Problem" }
    post:
      operationId: createWorkspace
      tags: [Workspaces]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ownerDiscordUserId, workerId]
              properties:
                ownerDiscordUserId: { type: string, minLength: 1 }
                workerId: { type: string, format: uuid }
      responses:
        "201":
          description: Workspace 已创建并绑定 Worker
          content: { application/json: { schema: { $ref: "#/components/schemas/IDResource" } } }
        default: { $ref: "#/components/responses/Problem" }
  /workspace-projects/{id}/forums:
    post:
      operationId: createWorkspaceProjectForum
      tags: [Workspaces]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkspaceProjectForumInput" }
      responses:
        "202": { description: Forum 创建已排队或历史 Forum 已恢复 }
        default: { $ref: "#/components/responses/Problem" }
  /workspace-forums/{id}/disable:
    post:
      operationId: disableWorkspaceForum
      tags: [Workspaces]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "202": { description: Forum 配对已停用并切换为只读 }
        default: { $ref: "#/components/responses/Problem" }
  /workspace-forums/{id}/enable:
    post:
      operationId: enableWorkspaceForum
      tags: [Workspaces]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "202": { description: Forum 配对已恢复 }
        default: { $ref: "#/components/responses/Problem" }
  /workspace-projects/{id}/forums/{forumId}/collaborators/{memberId}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: forumId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: memberId, in: path, required: true, schema: { type: string } }
    put:
      operationId: putWorkspaceProjectForumCollaborator
      tags: [Workspaces]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [accessLevel]
              properties: { accessLevel: { type: string, enum: [readonly, operator] } }
      responses:
        "204": { description: 已更新 }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteWorkspaceProjectForumCollaborator
      tags: [Workspaces]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "204": { description: 已删除 }
        default: { $ref: "#/components/responses/Problem" }
  /discord/forums/{forumId}/access/{memberId}:
    parameters:
      - { name: forumId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: memberId, in: path, required: true, schema: { type: string } }
    put:
      operationId: putDiscordForumAccess
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [accessLevel]
              properties: { accessLevel: { type: string, enum: [readonly, operator] } }
      responses:
        "204": { description: 已更新 }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteDiscordForumAccess
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "204": { description: 已删除 }
        default: { $ref: "#/components/responses/Problem" }
  /discord/github/bind:
    post:
      operationId: startDiscordGitHubBind
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [guildId, discordUserId]
              properties: { guildId: { type: string }, discordUserId: { type: string } }
      responses:
        "200":
          description: 一次性绑定链接
          content:
            application/json:
              schema:
                type: object
                required: [url]
                properties: { url: { type: string, format: uri } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/github/bind/callback:
    get:
      operationId: completeDiscordGitHubBind
      tags: [Discord]
      security: []
      parameters:
        - { name: state, in: query, required: true, schema: { type: string } }
        - { name: code, in: query, required: true, schema: { type: string } }
      responses:
        "200": { description: 绑定完成 }
        default: { $ref: "#/components/responses/Problem" }
  /discord/github/unbind:
    post:
      operationId: unbindDiscordGitHub
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [guildId, discordUserId, confirmed]
              properties:
                guildId: { type: string }
                discordUserId: { type: string }
                confirmed: { type: boolean }
      responses:
        "204": { description: 已解绑 }
        default: { $ref: "#/components/responses/Problem" }
  /events/stream:
    get:
      operationId: streamEvents
      tags: [Resources]
      responses:
        "200":
          description: Server-Sent Events
          content:
            text/event-stream:
              schema: { type: string }
  /repositories:
    get: { $ref: "#/components/pathItems/ListRepositories" }
    post:
      operationId: createRepository
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RepositoryInput" }
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IDResource" }
        default: { $ref: "#/components/responses/Problem" }
  /agent-profiles:
    get: { $ref: "#/components/pathItems/ListAgentProfiles" }
    post: { $ref: "#/components/pathItems/CreateGeneric" }
  /trigger-rules:
    get: { $ref: "#/components/pathItems/ListTriggerRules" }
    post:
      operationId: createTriggerRule
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TriggerRuleInput" }
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IDResource" }
        default: { $ref: "#/components/responses/Problem" }
  /work-items:
    get: { $ref: "#/components/pathItems/ListWorkItems" }
  /threads:
    get: { $ref: "#/components/pathItems/ListThreads" }
  /jobs:
    get: { $ref: "#/components/pathItems/ListJobs" }
  /workers:
    get:
      operationId: listWorkers
      tags: [Workers]
      responses:
        "200":
          description: 宿主 Worker 列表
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties: { items: { type: array, items: { $ref: "#/components/schemas/Worker" } } }
    post:
      operationId: createWorker
      tags: [Workers]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/WorkerInput" } } }
      responses:
        "201":
          description: Worker 与一次性注册 Token
          content: { application/json: { schema: { $ref: "#/components/schemas/WorkerCreated" } } }
  /workers/{id}/enrollments:
    post:
      operationId: createWorkerEnrollment
      tags: [Workers]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "201": { description: 一次性注册 Token, content: { application/json: { schema: { $ref: "#/components/schemas/EnrollmentToken" } } } }
  /workers/{id}:
    delete:
      operationId: deleteWorker
      tags: [Workers]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses: { "204": { description: 已删除 }, default: { $ref: "#/components/responses/Problem" } }
  /workers/{id}/enabled:
    put:
      operationId: setWorkerEnabled
      tags: [Workers]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [enabled], properties: { enabled: { type: boolean } } } } } }
      responses: { "204": { description: 已更新 }, default: { $ref: "#/components/responses/Problem" } }
  /settings/workers:
    get:
      operationId: getWorkerDefaults
      tags: [Workers]
      responses: { "200": { description: 默认 Worker, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerDefaults" } } } } }
    put:
      operationId: putWorkerDefaults
      tags: [Workers]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerDefaults" } } } }
      responses: { "204": { description: 已保存 }, default: { $ref: "#/components/responses/Problem" } }
  /ssh/credentials:
    get:
      operationId: listSSHCredentials
      tags: [SSH]
      responses:
        "200":
          description: SSH 凭证列表，不包含私钥或口令
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredentialList" } } }
        default: { $ref: "#/components/responses/Problem" }
    post:
      operationId: createSSHCredential
      tags: [SSH]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredentialCreateInput" } } }
      responses:
        "201":
          description: 已创建的脱敏 SSH 凭证
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredential" } } }
        default: { $ref: "#/components/responses/Problem" }
  /ssh/credentials/{id}:
    put:
      operationId: updateSSHCredential
      tags: [SSH]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredentialUpdateInput" } } }
      responses:
        "200":
          description: 已更新的脱敏 SSH 凭证
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredential" } } }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteSSHCredential
      tags: [SSH]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "204": { description: 已删除 }
        default: { $ref: "#/components/responses/Problem" }
  /ssh/hosts:
    get:
      operationId: listSSHHosts
      tags: [SSH]
      responses:
        "200":
          description: SSH 主机列表
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostList" } } }
        default: { $ref: "#/components/responses/Problem" }
    post:
      operationId: createSSHHost
      tags: [SSH]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostInput" } } }
      responses:
        "201":
          description: 已创建的 SSH 主机
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHHost" } } }
        default: { $ref: "#/components/responses/Problem" }
  /ssh/hosts/import:
    post:
      operationId: importSSHHosts
      tags: [SSH]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostImportInput" } } }
      responses:
        "201":
          description: 已批量导入的 SSH 主机
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostList" } } }
        default: { $ref: "#/components/responses/Problem" }
  /ssh/hosts/{id}:
    put:
      operationId: updateSSHHost
      tags: [SSH]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostInput" } } }
      responses:
        "200":
          description: 已更新的 SSH 主机
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHHost" } } }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteSSHHost
      tags: [SSH]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "204": { description: 已删除 }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/enroll:
    servers: [{ url: / }]
    post:
      operationId: enrollWorker
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: []
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerEnrollRequest" } } } }
      responses: { "200": { description: 长期 Worker 凭据, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerEnrollResponse" } } } }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/heartbeat:
    servers: [{ url: / }]
    post:
      operationId: workerHeartbeat
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ workerBearer: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerHeartbeat" } } } }
      responses:
        "204": { description: 心跳已记录 }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/claims:
    servers: [{ url: / }]
    post:
      operationId: workerClaim
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ workerBearer: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerClaimRequest" } } } }
      responses:
        "200": { description: 可选任务租约, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerClaimResponse" } } } }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/tunnels/claim:
    servers: [{ url: / }]
    post:
      operationId: workerClaimAppServerTunnel
      tags: [Worker Protocol]
      security: [{ workerBearer: [] }]
      responses:
        "200": { description: 可选 App Server 透明隧道, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/tunnels/{id}/connect:
    servers: [{ url: / }]
    get:
      operationId: workerConnectAppServerTunnel
      tags: [Worker Protocol]
      security: [{ workerBearer: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      responses:
        "101": { description: WebSocket 已升级 }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/materializations/claim:
    servers: [{ url: / }]
    post:
      operationId: workerClaimMaterialization
      tags: [Worker Protocol]
      security: [{ workerBearer: [] }]
      responses:
        "200": { description: 可选附件 materialization, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/materializations/{id}/content:
    servers: [{ url: / }]
    get:
      operationId: workerDownloadMaterialization
      tags: [Worker Protocol]
      security: [{ workerBearer: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: X-Materialization-Lease-Token, in: header, required: true, schema: { type: string } }
      responses:
        "200": { description: 待 materialize 附件, content: { application/octet-stream: { schema: { type: string, format: binary } } } }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/materializations/{id}/complete:
    servers: [{ url: / }]
    post:
      operationId: workerCompleteMaterialization
      tags: [Worker Protocol]
      security: [{ workerBearer: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "204": { description: materialization 已完成 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/materializations/{id}/fail:
    servers: [{ url: / }]
    post:
      operationId: workerFailMaterialization
      tags: [Worker Protocol]
      security: [{ workerBearer: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "204": { description: materialization 失败已记录 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/ssh-configuration:
    servers: [{ url: / }]
    get:
      operationId: workerSSHConfiguration
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ workerBearer: [] }]
      responses:
        "200": { description: 当前 Worker 的出站 SSH 配置, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerSSHConfiguration" } } } }
        "304": { description: 配置未变化 }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/workspace:
    servers: [{ url: / }]
    get:
      operationId: workerWorkspace
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ workerBearer: [] }]
      responses:
        "200": { description: 当前 Worker 可选 Workspace, content: { application/json: { schema: { type: object, nullable: true, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/workspace/projects/snapshot:
    servers: [{ url: / }]
    post:
      operationId: workerWorkspaceProjectSnapshot
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ workerBearer: [] }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "204": { description: 项目快照已保存 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/heartbeat:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerRunHeartbeat
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { $ref: "#/components/requestBodies/WorkerJSON" }
      responses: { "200": { $ref: "#/components/responses/WorkerJSON" }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/events:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerRunEvents
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerEventsRequest" } } } }
      responses: { "204": { description: 事件已保存 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/complete:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerRunComplete
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerCompleteRequest" } } } }
      responses: { "204": { description: Run 已完成 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/fail:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerRunFail
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerFailRequest" } } } }
      responses: { "204": { description: Run 失败状态已保存 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/thread:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerSetThread
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { $ref: "#/components/requestBodies/WorkerJSON" }
      responses: { "204": { description: Thread 已绑定 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/submission:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerRecordSubmission
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { $ref: "#/components/requestBodies/WorkerJSON" }
      responses: { "204": { description: Submission 已记录 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/confirm:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerConfirmTurn
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { $ref: "#/components/requestBodies/WorkerJSON" }
      responses: { "204": { description: Turn 已确认 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/workspace-state:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerWorkspaceState
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { $ref: "#/components/requestBodies/WorkerJSON" }
      responses: { "204": { description: Workspace 状态已保存 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/tools/call:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerToolCall
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { $ref: "#/components/requestBodies/WorkerJSON" }
      responses: { "200": { $ref: "#/components/responses/WorkerJSON" }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/git-credential:
    servers: [{ url: / }]
    post:
      tags: [Worker Protocol]
      operationId: workerGitCredential
      security: [{ workerBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { $ref: "#/components/requestBodies/WorkerJSON" }
      responses: { "200": { $ref: "#/components/responses/WorkerJSON" }, default: { $ref: "#/components/responses/Problem" } }
  /worktrees:
    get: { $ref: "#/components/pathItems/ListWorktrees" }
  /audit-logs:
    get: { $ref: "#/components/pathItems/ListAuditLogs" }
  /installations:
    get: { $ref: "#/components/pathItems/ListInstallations" }
  /repo-caches:
    get: { $ref: "#/components/pathItems/ListRepoCaches" }
  /system/status:
    get:
      operationId: getSystemStatus
      tags: [Resources]
      responses:
        "200":
          description: 系统状态
          content:
            application/json:
              schema: { type: object, additionalProperties: { type: integer, format: int64 } }
        default: { $ref: "#/components/responses/Problem" }
  /client/auth/login:
    post:
      operationId: clientLogin
      tags: [Client Protocol]
      security: []
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: "#/components/schemas/LoginRequest" } }
      responses:
        "200": { description: 客户端临时登录凭证, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/device-pairings/{id}/claim:
    post:
      operationId: clientClaimDevicePairing
      tags: [Client Protocol]
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [pairingSecret, deviceId, name, platform, credentialHash]
              properties:
                pairingSecret: { type: string }
                deviceId: { type: string, format: uuid }
                name: { type: string, maxLength: 120 }
                platform: { type: string }
                credentialHash: { type: string, minLength: 64, maxLength: 64 }
      responses:
        "200": { description: 等待管理员确认, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/device-pairings/{id}/status:
    get:
      operationId: clientDevicePairingStatus
      tags: [Client Protocol]
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: Authorization, in: header, required: true, schema: { type: string, pattern: '^Pairing ' } }
      responses:
        "200": { description: 设备绑定状态, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/bootstrap:
    get:
      operationId: clientBootstrap
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      responses:
        "200": { description: 协议 v4 启动快照, content: { application/json: { schema: { $ref: "#/components/schemas/ClientBootstrap" } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/tunnels:
    post:
      operationId: clientCreateAppServerTunnel
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { type: object, required: [workspaceId], properties: { workspaceId: { type: string, format: uuid } } } }
      responses:
        "201": { description: 单次透明隧道 ticket, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/tunnels/{ticket}/connect:
    get:
      operationId: clientConnectAppServerTunnel
      tags: [Client Protocol]
      security: []
      parameters:
        - { name: ticket, in: path, required: true, schema: { type: string } }
      responses:
        "101": { description: WebSocket 已升级 }
        default: { $ref: "#/components/responses/Problem" }
  /client/materializations:
    post:
      operationId: clientCreateMaterialization
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [workspaceId, clientId, file]
              properties:
                workspaceId: { type: string, format: uuid }
                clientId: { type: string }
                file: { type: string, format: binary }
      responses:
        "201": { description: Worker 本地附件路径, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/legacy/archive/sessions:
    get:
      operationId: clientListLegacyArchive
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      responses:
        "200": { description: 只读旧会话归档, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/legacy/archive/sessions/{id}:
    get:
      operationId: clientGetLegacyArchive
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: 只读旧会话详情与消息, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/device/push-token:
    put:
      operationId: clientPutPushToken
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { type: object, required: [token, platform, appEnvironment], properties: { token: { type: string }, platform: { type: string, enum: [ios, android] }, appEnvironment: { type: string, enum: [development, preview, production] } } } }
      responses: { "204": { description: 已注册 }, default: { $ref: "#/components/responses/Problem" } }
    delete:
      operationId: clientDeletePushToken
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: token, in: query, required: true, schema: { type: string } }
      responses: { "204": { description: 已删除 }, default: { $ref: "#/components/responses/Problem" } }
  /client/device:
    delete:
      operationId: clientDeleteDevice
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      responses: { "204": { description: 当前设备已自撤销 }, default: { $ref: "#/components/responses/Problem" } }
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: tyrs_hand_session
    workerBearer:
      type: http
      scheme: bearer
      bearerFormat: opaque-worker-credential
    clientBearer:
      type: http
      scheme: bearer
      bearerFormat: tyrs-hand-device-token
  parameters:
    CSRFToken:
      name: X-CSRF-Token
      in: header
      required: true
      schema: { type: string }
    Cursor:
      name: cursor
      in: query
      required: false
      schema: { type: string }
    Limit:
      name: limit
      in: query
      required: false
      schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
    WorkerResourceID:
      name: id
      in: path
      required: true
      schema: { type: string, format: uuid }
  requestBodies:
    WorkerJSON:
      required: true
      content:
        application/json:
          schema: { type: object, additionalProperties: true }
  responses:
    Problem:
      description: RFC 9457 Problem Details
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    ResourceList:
      description: 游标分页资源列表
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ResourceList" }
    WorkerJSON:
      description: Worker 直连协议响应
      content:
        application/json:
          schema: { type: object, additionalProperties: true }
  pathItems:
    ListRepositories:
      operationId: listRepositories
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListAgentProfiles:
      operationId: listAgentProfiles
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListTriggerRules:
      operationId: listTriggerRules
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListWorkItems:
      operationId: listWorkItems
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListThreads:
      operationId: listThreads
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListJobs:
      operationId: listJobs
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListWorktrees:
      operationId: listWorktrees
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListAuditLogs:
      operationId: listAuditLogs
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListInstallations:
      operationId: listInstallations
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListRepoCaches:
      operationId: listRepoCaches
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    CreateGeneric:
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IDResource" }
        default: { $ref: "#/components/responses/Problem" }
  schemas:
    ClientProject:
      type: object
      required: [id, workspaceId, name, relativePath, absolutePath, kind,
        availabilityStatus, branch, dirty]
      properties:
        id: { type: string, format: uuid }
        workspaceId: { type: string, format: uuid }
        name: { type: string }
        relativePath: { type: string }
        absolutePath: { type: string }
        kind: { type: string }
        availabilityStatus: { type: string }
        branch: { type: string, nullable: true }
        dirty: { type: boolean }
    ClientModel:
      type: object
      required: [id, displayName, description, supportedReasoningEfforts,
        defaultReasoningEffort, isDefault]
      properties:
        id: { type: string }
        model: { type: string }
        displayName: { type: string }
        description: { type: string }
        supportedReasoningEfforts:
          type: array
          items:
            type: object
            required: [reasoningEffort, description]
            properties:
              reasoningEffort: { type: string, minLength: 1 }
              description: { type: string }
        defaultReasoningEffort: { type: string, minLength: 1 }
        inputModalities: { type: array, items: { type: string } }
        additionalSpeedTiers: { type: array, items: { type: string } }
        serviceTiers:
          type: array
          items:
            type: object
            required: [id, name, description]
            properties:
              id: { type: string }
              name: { type: string }
              description: { type: string }
        defaultServiceTier: { type: string, nullable: true }
        isDefault: { type: boolean }
        hidden: { type: boolean }
    ClientBootstrap:
      type: object
      required: [serverId, protocolVersion, user, capabilities, workspaces, projects]
      properties:
        serverId: { type: string, format: uuid }
        protocolVersion: { type: integer, enum: [4] }
        user:
          type: object
          required: [id, username]
          properties:
            id: { type: string, format: uuid }
            username: { type: string }
        capabilities: { type: object, additionalProperties: { type: boolean } }
        workspaces: { type: array, items: { type: object, additionalProperties: true } }
        projects: { type: array, items: { $ref: "#/components/schemas/ClientProject" } }
    Worker:
      type: object
      required: [id, name, roles, enabled, maxConcurrentJobs, protocolVersion, status, metadata]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        roles: { type: array, items: { type: string, enum: [github, discord] } }
        enabled: { type: boolean }
        maxConcurrentJobs: { type: integer, minimum: 1 }
        protocolVersion: { type: integer }
        workerVersion: { type: string }
        status: { type: string, enum: [pending, online, offline, disabled, incompatible, error] }
        heartbeatAt: { type: string, format: date-time }
        lastError: { type: string }
        metadata: { type: object, additionalProperties: true }
    WorkerInput:
      type: object
      required: [name, roles, maxConcurrentJobs]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        roles: { type: array, minItems: 1, items: { type: string, enum: [github, discord] } }
        maxConcurrentJobs: { type: integer, minimum: 1 }
    EnrollmentToken:
      type: object
      required: [enrollmentToken, expiresIn]
      properties:
        enrollmentToken: { type: string, writeOnly: true }
        expiresIn: { type: integer, enum: [900] }
    WorkerCreated:
      allOf:
        - { $ref: "#/components/schemas/EnrollmentToken" }
        - type: object
          required: [worker]
          properties: { worker: { $ref: "#/components/schemas/Worker" } }
    WorkerDefaults:
      type: object
      properties:
        githubWorkerId: { type: string, format: uuid, nullable: true }
        discordWorkerId: { type: string, format: uuid, nullable: true }
    SSHCredential:
      type: object
      required: [id, name, publicKey, fingerprint, enabled, version, hostCount, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        publicKey: { type: string, readOnly: true }
        fingerprint: { type: string, readOnly: true }
        enabled: { type: boolean }
        version: { type: integer, format: int64, readOnly: true }
        hostCount: { type: integer, readOnly: true }
        updatedAt: { type: string, format: date-time, readOnly: true }
    SSHCredentialList:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/SSHCredential" } }
    SSHCredentialCreateInput:
      type: object
      required: [name, privateKey, enabled]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        privateKey: { type: string, minLength: 1, writeOnly: true }
        passphrase: { type: string, writeOnly: true }
        enabled: { type: boolean }
    SSHCredentialUpdateInput:
      type: object
      required: [name, enabled]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        privateKey:
          type: string
          writeOnly: true
          description: 非空时显式轮换私钥；空字符串表示保持现有私钥
        passphrase: { type: string, writeOnly: true }
        enabled: { type: boolean }
    SSHHost:
      type: object
      required: [id, alias, hostname, port, username, credentialId, credentialName, workerIds, enabled, updatedAt]
      properties:
        id: { type: string, format: uuid }
        alias: { type: string }
        hostname: { type: string }
        port: { type: integer, minimum: 1, maximum: 65535 }
        username: { type: string }
        credentialId: { type: string, format: uuid }
        credentialName: { type: string, readOnly: true }
        proxyJumpHostId: { type: string, format: uuid }
        proxyJumpAlias: { type: string, readOnly: true }
        workerIds: { type: array, items: { type: string, format: uuid } }
        enabled: { type: boolean }
        updatedAt: { type: string, format: date-time, readOnly: true }
    SSHHostInput:
      type: object
      required: [alias, hostname, port, username, credentialId, workerIds, enabled]
      properties:
        alias: { type: string, minLength: 1, maxLength: 128, pattern: "^[A-Za-z0-9._-]+$" }
        hostname: { type: string, minLength: 1, maxLength: 255 }
        port: { type: integer, minimum: 1, maximum: 65535 }
        username: { type: string, minLength: 1, maxLength: 128 }
        credentialId: { type: string, format: uuid }
        proxyJumpHostId: { type: string, format: uuid }
        workerIds: { type: array, items: { type: string, format: uuid }, uniqueItems: true }
        enabled: { type: boolean }
    SSHHostImportItem:
      type: object
      required: [alias, hostname, port, username]
      properties:
        alias: { type: string, minLength: 1, maxLength: 128, pattern: "^[A-Za-z0-9._-]+$" }
        hostname: { type: string, minLength: 1, maxLength: 255 }
        port: { type: integer, minimum: 1, maximum: 65535 }
        username: { type: string, minLength: 1, maxLength: 128 }
        proxyJumpAlias: { type: string, minLength: 1, maxLength: 128, pattern: "^[A-Za-z0-9._-]+$" }
    SSHHostImportInput:
      type: object
      required: [credentialId, workerIds, enabled, hosts]
      properties:
        credentialId: { type: string, format: uuid }
        workerIds: { type: array, items: { type: string, format: uuid }, uniqueItems: true }
        enabled: { type: boolean }
        hosts:
          type: array
          minItems: 1
          maxItems: 100
          items: { $ref: "#/components/schemas/SSHHostImportItem" }
    SSHHostList:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/SSHHost" } }
    WorkerSSHCredential:
      type: object
      required: [id, privateKey, publicKey, fingerprint]
      properties:
        id: { type: string, format: uuid }
        privateKey: { type: string, writeOnly: true }
        passphrase: { type: string, writeOnly: true }
        publicKey: { type: string }
        fingerprint: { type: string }
    WorkerSSHHost:
      type: object
      required: [alias, hostname, port, username, credentialId]
      properties:
        alias: { type: string }
        hostname: { type: string }
        port: { type: integer, minimum: 1, maximum: 65535 }
        username: { type: string }
        credentialId: { type: string, format: uuid }
        proxyJumpAlias: { type: string }
    WorkerSSHConfiguration:
      type: object
      required: [revision, credentials, hosts]
      properties:
        revision: { type: string }
        credentials: { type: array, items: { $ref: "#/components/schemas/WorkerSSHCredential" } }
        hosts: { type: array, items: { $ref: "#/components/schemas/WorkerSSHHost" } }
    WorkerEnrollRequest:
      type: object
      required: [token]
      properties: { token: { type: string, writeOnly: true } }
    WorkerEnrollResponse:
      type: object
      required: [workerId, credential, protocolVersion]
      properties:
        workerId: { type: string, format: uuid }
        credential: { type: string, writeOnly: true }
        protocolVersion: { type: integer }
    WorkerHeartbeat:
      type: object
      required: [workerVersion, protocolVersion]
      properties:
        workerVersion: { type: string }
        protocolVersion: { type: integer }
        metadata: { type: object, additionalProperties: true }
    WorkerClaimRequest:
      type: object
      required: [role, wait]
      properties:
        role: { type: string, enum: [github] }
        wait: { type: boolean }
    WorkerClaimResponse:
      type: object
      properties:
        task: { type: object, additionalProperties: true }
    WorkerRunLease:
      type: object
      required: [leaseToken, leaseEpoch]
      properties:
        leaseToken: { type: string, writeOnly: true }
        leaseEpoch: { type: integer, format: int64 }
    WorkerEvent:
      type: object
      required: [sequence, type, payload]
      properties:
        sequence: { type: integer, format: int64, minimum: 1 }
        type: { type: string }
        payload: { type: object, additionalProperties: true }
    WorkerEventsRequest:
      allOf:
        - { $ref: "#/components/schemas/WorkerRunLease" }
        - type: object
          required: [events]
          properties:
            events: { type: array, items: { $ref: "#/components/schemas/WorkerEvent" } }
    WorkerCompleteRequest:
      allOf:
        - { $ref: "#/components/schemas/WorkerRunLease" }
        - type: object
          required: [idempotencyKey, result]
          properties:
            idempotencyKey: { type: string }
            result: { type: object, additionalProperties: true }
    WorkerFailRequest:
      allOf:
        - { $ref: "#/components/schemas/WorkerRunLease" }
        - type: object
          required: [idempotencyKey, code, message]
          properties:
            idempotencyKey: { type: string }
            code: { type: string }
            message: { type: string }
    ProblemDetails:
      type: object
      required: [type, title, status]
      properties:
        type: { type: string, format: uri-reference }
        title: { type: string }
        status: { type: integer, minimum: 400, maximum: 599 }
        detail: { type: string }
        instance: { type: string, format: uri-reference }
        requestId: { type: string }
    SetupStatus:
      type: object
      required: [setupRequired, githubConfigured]
      properties:
        setupRequired: { type: boolean }
        githubConfigured: { type: boolean }
    SetupRequest:
      type: object
      required: [setupToken, username, password]
      properties:
        setupToken: { type: string, minLength: 16 }
        username: { type: string, minLength: 3, maxLength: 64 }
        password: { type: string, minLength: 12, maxLength: 256 }
    SetupResult:
      type: object
      required: [totpSecret, provisioningUri, recoveryCodes]
      properties:
        totpSecret: { type: string }
        provisioningUri: { type: string, format: uri }
        recoveryCodes: { type: array, items: { type: string } }
    LoginRequest:
      type: object
      required: [username, password, totp]
      properties:
        username: { type: string }
        password: { type: string }
        totp: { type: string, pattern: "^[0-9]{6}$" }
    LoginResult:
      type: object
      required: [username, csrfToken, expiresAt]
      properties:
        username: { type: string }
        csrfToken: { type: string }
        expiresAt: { type: string, format: date-time }
    Administrator:
      type: object
      required: [id, username, csrfToken, expiresAt]
      properties:
        id: { type: string, format: uuid }
        username: { type: string }
        csrfToken: { type: string }
        expiresAt: { type: string, format: date-time }
    GitHubAppStatus:
      type: object
      required: [configured]
      properties:
        configured: { type: boolean }
        appId: { type: integer, format: int64 }
        clientId: { type: string }
        appSlug: { type: string }
    GitHubAppInput:
      type: object
      required: [appId, appSlug, privateKey, webhookSecret]
      properties:
        appId: { type: integer, format: int64, minimum: 1 }
        clientId: { type: string }
        appSlug: { type: string }
        privateKey: { type: string, writeOnly: true }
        webhookSecret: { type: string, writeOnly: true }
        clientSecret: { type: string, writeOnly: true }
    RepositoryInput:
      type: object
      required: [installationExternalId, accountLogin, repositoryExternalId, owner, name, defaultBranch, cloneUrl]
      properties:
        installationExternalId: { type: integer, format: int64 }
        accountLogin: { type: string }
        accountType: { type: string, default: Organization }
        repositoryExternalId: { type: integer, format: int64 }
        owner: { type: string }
        name: { type: string }
        defaultBranch: { type: string }
        cloneUrl: { type: string, format: uri }
    TriggerRuleInput:
      type: object
      required: [repositoryId, agentProfileId, name, eventName, triggerKind, instruction]
      properties:
        repositoryId: { type: string, format: uuid }
        agentProfileId: { type: string, format: uuid }
        name: { type: string, minLength: 1, maxLength: 128 }
        eventName: { type: string, minLength: 1 }
        action: { type: string }
        enabled: { type: boolean }
        priority: { type: integer, default: 100 }
        triggerKind:
          type: string
          enum: [event, label, slash_command, mention_command, legacy_mention]
        triggerValue:
          type: string
          description: Label 名或不带斜杠的命令名；event、mention_command 与 legacy_mention 留空。
        actorMinPermission:
          type: string
          enum: [read, triage, write, maintain, admin]
          default: triage
        instruction: { type: string, minLength: 1 }
        skills: { type: array, items: { type: string } }
        allowedTools: { type: array, items: { type: string } }
        dangerousActions: { type: array, items: { type: string } }
        filters: { type: object, additionalProperties: true }
    GitHubAgentInstructions:
      type: object
      required: [content]
      properties:
        content: { type: string, maxLength: 262144 }
    GitHubAgentPreferences:
      type: object
      properties:
        model: { type: string, nullable: true, maxLength: 128 }
        reasoningEffort: { type: string, nullable: true, maxLength: 64 }
        serviceTier: { type: string, nullable: true, enum: [standard, fast] }
    GitHubAgentEffectivePreferences:
      type: object
      required: [model, reasoningEffort, serviceTier]
      properties:
        model: { type: string }
        reasoningEffort: { type: string }
        serviceTier: { type: string, enum: [standard, fast] }
    GitHubAgentRepositorySettings:
      type: object
      required: [id, owner, name, settings, effective]
      properties:
        id: { type: string, format: uuid }
        owner: { type: string }
        name: { type: string }
        settings: { $ref: "#/components/schemas/GitHubAgentPreferences" }
        effective: { $ref: "#/components/schemas/GitHubAgentEffectivePreferences" }
    GitHubAgentSettingsList:
      type: object
      required: [items, models]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/GitHubAgentRepositorySettings" } }
        models: { type: array, items: { $ref: "#/components/schemas/ClientModel" } }
    DiscordSettings:
      type: object
      required: [guildId, enabled, communityEnabled, tokenConfigured]
      properties:
        guildId: { type: string }
        enabled: { type: boolean }
        communityEnabled: { type: boolean }
        applicationId: { type: string }
        botUserId: { type: string }
        tokenConfigured: { type: boolean }
    DiscordSettingsInput:
      type: object
      required: [guildId, enabled]
      properties:
        guildId: { type: string, pattern: "^[0-9]+$" }
        enabled: { type: boolean }
        botToken: { type: string, writeOnly: true }
        applicationId: { type: string, pattern: "^[0-9]*$" }
        botUserId: { type: string, pattern: "^[0-9]*$" }
    DiscordStatus:
      type: object
      required: [configured, enabled, gatewayStatus, pendingOutbox, failedOutbox, pendingInitializationOperations]
      properties:
        configured: { type: boolean }
        enabled: { type: boolean }
        gatewayStatus: { type: string }
        gatewayError: { type: string }
        lastGatewayAt: { type: string, format: date-time }
        pendingOutbox: { type: integer, format: int64 }
        failedOutbox: { type: integer, format: int64 }
        pendingInitializationOperations: { type: integer, format: int64 }
    DiscordInitializationInput:
      type: object
      required: [mode]
      properties:
        mode: { type: string, enum: [incremental, fresh] }
        confirmation: { type: string }
    DiscordConflict:
      type: object
      required: [name, reason]
      properties:
        name: { type: string }
        reason: { type: string }
    DiscordPreflight:
      type: object
      required: [guildId, mode, creates, updates, deletes, conflicts, missingPermissions, channelCount, safe]
      properties:
        guildId: { type: string }
        mode: { type: string, enum: [incremental, fresh] }
        creates: { type: array, items: { type: string } }
        updates: { type: array, items: { type: string } }
        deletes: { type: array, items: { type: string } }
        conflicts: { type: array, items: { $ref: "#/components/schemas/DiscordConflict" } }
        missingPermissions: { type: array, items: { type: string } }
        channelCount: { type: integer }
        safe: { type: boolean }
    DiscordInitialization:
      type: object
      required: [id, guildId, mode, status, preflight, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        guildId: { type: string }
        mode: { type: string }
        status: { type: string }
        preflight: { $ref: "#/components/schemas/DiscordPreflight" }
        error: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    DiscordMember:
      type: object
      required: [guildId, discordUserId, username, displayName, bound]
      properties:
        guildId: { type: string }
        discordUserId: { type: string }
        username: { type: string }
        displayName: { type: string }
        bound: { type: boolean }
        githubLogin: { type: string }
    WorkspaceForumCollaborator:
      type: object
      required: [forumId, memberId, accessLevel, administratorBypass]
      properties:
        forumId: { type: string, format: uuid }
        memberId: { type: string }
        accessLevel: { type: string, enum: [readonly, operator] }
        administratorBypass: { type: boolean }
    WorkspaceForum:
      type: object
      required: [id, name, discordId, bindingStatus, collaborators]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        discordId: { type: string }
        bindingStatus: { type: string, enum: [active, inactive] }
        collaborators:
          type: array
          items: { $ref: "#/components/schemas/WorkspaceForumCollaborator" }
    WorkspaceProject:
      type: object
      required: [id, name, relativePath, projectKind, availabilityStatus, dirty, lastSeenAt, forums]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        relativePath: { type: string }
        desiredRelativePath: { type: string }
        projectKind: { type: string, enum: [directory, git] }
        availabilityStatus: { type: string, enum: [available, missing] }
        branch: { type: string }
        headSha: { type: string }
        dirty: { type: boolean }
        remoteUrl: { type: string }
        lastSeenAt: { type: string, format: date-time }
        scanError: { type: string }
        forums:
          type: array
          items: { $ref: "#/components/schemas/WorkspaceForum" }
    Workspace:
      type: object
      required: [id, ownerDiscordUserId, ownerName, projects]
      properties:
        id: { type: string, format: uuid }
        ownerDiscordUserId: { type: string }
        ownerName: { type: string }
        workerId: { type: string, format: uuid }
        projectsScannedAt: { type: string, format: date-time }
        projectScanError: { type: string }
        projects:
          type: array
          items: { $ref: "#/components/schemas/WorkspaceProject" }
    WorkspaceList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/Workspace" }
    WorkspaceProjectForumInput:
      type: object
      required: [mode]
      properties:
        mode: { type: string, enum: [new, restore] }
        forumId: { type: string, format: uuid }
        name: { type: string, maxLength: 100 }
    IDResource:
      type: object
      required: [id]
      properties:
        id: { type: string, format: uuid }
    ResourceList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: { type: object, additionalProperties: true }
        nextCursor: { type: string }
security:
  - sessionCookie: []
