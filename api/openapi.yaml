openapi: 3.1.0
info:
  title: tyrs-hand Management API
  version: 0.1.0
  license:
    name: MIT
    identifier: MIT
servers:
  - url: /api/v1
tags:
  - name: Auth
  - name: GitHub
  - name: Resources
  - name: Settings
  - name: Discord
  - name: Development Environments
  - name: Execution Nodes
  - name: SSH
  - name: Worker Protocol
  - name: Client Protocol
paths:
  /webhooks/github:
    post:
      operationId: receiveGitHubWebhook
      tags: [GitHub]
      servers: [{ url: / }]
      security: []
      parameters:
        - { name: X-Hub-Signature-256, in: header, required: true, schema: { type: string } }
        - { name: X-GitHub-Delivery, in: header, required: true, schema: { type: string } }
        - { name: X-GitHub-Event, in: header, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "202":
          description: 已持久化
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        default: { $ref: "#/components/responses/Problem" }
  /internal/v1/tools/call:
    post:
      operationId: callInternalTool
      tags: [Resources]
      servers: [{ url: / }]
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "200":
          description: Tool Call 结果
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        default: { $ref: "#/components/responses/Problem" }
  /setup/status:
    get:
      operationId: getSetupStatus
      tags: [Auth]
      security: []
      responses:
        "200":
          description: Setup 状态
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SetupStatus" }
  /setup/admin:
    post:
      operationId: setupAdministrator
      tags: [Auth]
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SetupRequest" }
      responses:
        "201":
          description: 管理员与恢复资料
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SetupResult" }
        default: { $ref: "#/components/responses/Problem" }
  /auth/login:
    post:
      operationId: login
      tags: [Auth]
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "200":
          description: 登录会话
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LoginResult" }
        default: { $ref: "#/components/responses/Problem" }
  /auth/me:
    get:
      operationId: getCurrentAdministrator
      tags: [Auth]
      responses:
        "200":
          description: 当前管理员
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Administrator" }
        default: { $ref: "#/components/responses/Problem" }
  /auth/logout:
    post:
      operationId: logout
      tags: [Auth]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "204": { description: 已退出 }
        default: { $ref: "#/components/responses/Problem" }
  /github/app:
    get:
      operationId: getGitHubApp
      tags: [GitHub]
      responses:
        "200":
          description: GitHub App 状态
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GitHubAppStatus" }
    put:
      operationId: putGitHubApp
      tags: [GitHub]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GitHubAppInput" }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /github/app/manifest:
    get:
      operationId: getGitHubAppManifest
      tags: [GitHub]
      responses:
        "200":
          description: Manifest 表单参数
          content:
            application/json:
              schema:
                type: object
                required: [url, manifest]
                properties:
                  url: { type: string, format: uri }
                  manifest: { type: string }
  /github/app/manifest/callback:
    get:
      operationId: githubAppManifestCallback
      tags: [GitHub]
      parameters:
        - name: code
          in: query
          required: true
          schema: { type: string }
      responses:
        "302": { description: 返回 GitHub 设置页 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/agent-provider:
    get:
      operationId: getAgentProviderSettings
      tags: [Settings]
      responses:
        "200":
          description: Agent Provider 设置
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AgentProviderSettings" }
    put:
      operationId: putAgentProviderSettings
      tags: [Settings]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AgentProviderSettingsInput" }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/agent-provider/chatgpt/login:
    post:
      operationId: startChatGPTLogin
      tags: [Settings]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "202":
          description: 已发起 ChatGPT Device Code 登录
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChatGPTLoginOperation" }
        default: { $ref: "#/components/responses/Problem" }
  /settings/agent-provider/chatgpt/login/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getChatGPTLogin
      tags: [Settings]
      responses:
        "200":
          description: ChatGPT Device Code 登录状态
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChatGPTLoginOperation" }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: cancelChatGPTLogin
      tags: [Settings]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "204": { description: 已取消登录 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/agent-provider/chatgpt/account:
    delete:
      operationId: logoutChatGPT
      tags: [Settings]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "204": { description: 已退出全局 ChatGPT 账号 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/global-agents:
    get:
      operationId: getGlobalAgents
      tags: [Settings]
      responses:
        "200":
          description: 注入所有 Codex Home 的全局 AGENTS.md
          content: { application/json: { schema: { $ref: "#/components/schemas/GlobalAgents" } } }
        default: { $ref: "#/components/responses/Problem" }
    put:
      operationId: putGlobalAgents
      tags: [Settings]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/GlobalAgents" } } }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/codex:
    get:
      operationId: listCodexSettings
      tags: [Settings]
      responses:
        "200":
          description: 仓库与 Discord Forum 的 Codex 设置
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CodexSettingsList" }
        default: { $ref: "#/components/responses/Problem" }
  /settings/codex/repositories/{id}:
    put:
      operationId: putRepositoryCodexSettings
      tags: [Settings]
      parameters:
        - { $ref: "#/components/parameters/CSRFToken" }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CodexPreferences" } } }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/codex/forums/{id}:
    put:
      operationId: putForumCodexSettings
      tags: [Settings]
      parameters:
        - { $ref: "#/components/parameters/CSRFToken" }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CodexPreferences" } } }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /settings/discord:
    get:
      operationId: getDiscordSettings
      tags: [Discord]
      responses:
        "200":
          description: Discord 设置
          content: { application/json: { schema: { $ref: "#/components/schemas/DiscordSettings" } } }
        default: { $ref: "#/components/responses/Problem" }
    put:
      operationId: putDiscordSettings
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/DiscordSettingsInput" } } }
      responses:
        "204": { description: 已保存 }
        default: { $ref: "#/components/responses/Problem" }
  /discord/status:
    get:
      operationId: getDiscordStatus
      tags: [Discord]
      responses:
        "200":
          description: Discord 运行状态
          content: { application/json: { schema: { $ref: "#/components/schemas/DiscordStatus" } } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/initializations/preflight:
    post:
      operationId: preflightDiscordInitialization
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/DiscordInitializationInput" } } }
      responses:
        "200":
          description: 零修改预检结果
          content: { application/json: { schema: { $ref: "#/components/schemas/DiscordPreflight" } } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/initializations:
    post:
      operationId: createDiscordInitialization
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/DiscordInitializationInput" } } }
      responses:
        "202":
          description: 初始化已排队
          content: { application/json: { schema: { $ref: "#/components/schemas/IDResource" } } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/initializations/{id}:
    get:
      operationId: getDiscordInitialization
      tags: [Discord]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: 初始化状态
          content: { application/json: { schema: { $ref: "#/components/schemas/DiscordInitialization" } } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/members:
    get:
      operationId: listDiscordMembers
      tags: [Discord]
      responses:
        "200":
          description: Discord 成员
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/DiscordMember" } }
        default: { $ref: "#/components/responses/Problem" }
  /development-environments:
    get:
      operationId: listDevelopmentEnvironments
      tags: [Development Environments]
      responses:
        "200":
          description: 长期开发环境
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DevelopmentEnvironmentList" }
        default: { $ref: "#/components/responses/Problem" }
    post:
      operationId: createDevelopmentEnvironment
      tags: [Development Environments]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ownerDiscordUserId]
              properties:
                ownerDiscordUserId: { type: string, minLength: 1 }
      responses:
        "202":
          description: 环境创建已排队
          content: { application/json: { schema: { $ref: "#/components/schemas/DevelopmentEnvironmentOperation" } } }
        default: { $ref: "#/components/responses/Problem" }
  /development-environments/{id}/rebase:
    post:
      operationId: rebaseDevelopmentEnvironment
      tags: [Development Environments]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "202": { description: 已排队 Rebase 到当前官方开发镜像 }
        default: { $ref: "#/components/responses/Problem" }
  /development-environments/{id}/ssh:
    put:
      operationId: putDevelopmentEnvironmentSSH
      tags: [Development Environments]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/DevelopmentEnvironmentSSHInput" } } }
      responses:
        "202": { description: SSH 配置已排队 }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteDevelopmentEnvironmentSSH
      tags: [Development Environments]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "202": { description: SSH 停用已排队 }
        default: { $ref: "#/components/responses/Problem" }
  /development-projects/{id}/forums:
    post:
      operationId: createDevelopmentProjectForum
      tags: [Development Environments]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DevelopmentProjectForumInput" }
      responses:
        "202": { description: Forum 创建已排队或历史 Forum 已恢复 }
        default: { $ref: "#/components/responses/Problem" }
  /development-forums/{id}/disable:
    post:
      operationId: disableDevelopmentForum
      tags: [Development Environments]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "202": { description: Forum 配对已停用并切换为只读 }
        default: { $ref: "#/components/responses/Problem" }
  /development-forums/{id}/enable:
    post:
      operationId: enableDevelopmentForum
      tags: [Development Environments]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "202": { description: Forum 配对已恢复 }
        default: { $ref: "#/components/responses/Problem" }
  /development-projects/{id}/forums/{forumId}/collaborators/{memberId}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: forumId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: memberId, in: path, required: true, schema: { type: string } }
    put:
      operationId: putDevelopmentProjectForumCollaborator
      tags: [Development Environments]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [accessLevel]
              properties: { accessLevel: { type: string, enum: [readonly, operator] } }
      responses:
        "204": { description: 已更新 }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteDevelopmentProjectForumCollaborator
      tags: [Development Environments]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "204": { description: 已删除 }
        default: { $ref: "#/components/responses/Problem" }
  /discord/forums/{forumId}/access/{memberId}:
    parameters:
      - { name: forumId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: memberId, in: path, required: true, schema: { type: string } }
    put:
      operationId: putDiscordForumAccess
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [accessLevel]
              properties: { accessLevel: { type: string, enum: [readonly, operator] } }
      responses:
        "204": { description: 已更新 }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteDiscordForumAccess
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      responses:
        "204": { description: 已删除 }
        default: { $ref: "#/components/responses/Problem" }
  /discord/github/bind:
    post:
      operationId: startDiscordGitHubBind
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [guildId, discordUserId]
              properties: { guildId: { type: string }, discordUserId: { type: string } }
      responses:
        "200":
          description: 一次性绑定链接
          content:
            application/json:
              schema:
                type: object
                required: [url]
                properties: { url: { type: string, format: uri } }
        default: { $ref: "#/components/responses/Problem" }
  /discord/github/bind/callback:
    get:
      operationId: completeDiscordGitHubBind
      tags: [Discord]
      security: []
      parameters:
        - { name: state, in: query, required: true, schema: { type: string } }
        - { name: code, in: query, required: true, schema: { type: string } }
      responses:
        "200": { description: 绑定完成 }
        default: { $ref: "#/components/responses/Problem" }
  /discord/github/unbind:
    post:
      operationId: unbindDiscordGitHub
      tags: [Discord]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [guildId, discordUserId, confirmed]
              properties:
                guildId: { type: string }
                discordUserId: { type: string }
                confirmed: { type: boolean }
      responses:
        "204": { description: 已解绑 }
        default: { $ref: "#/components/responses/Problem" }
  /events/stream:
    get:
      operationId: streamEvents
      tags: [Resources]
      responses:
        "200":
          description: Server-Sent Events
          content:
            text/event-stream:
              schema: { type: string }
  /repositories:
    get: { $ref: "#/components/pathItems/ListRepositories" }
    post:
      operationId: createRepository
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RepositoryInput" }
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IDResource" }
        default: { $ref: "#/components/responses/Problem" }
  /agent-profiles:
    get: { $ref: "#/components/pathItems/ListAgentProfiles" }
    post: { $ref: "#/components/pathItems/CreateGeneric" }
  /trigger-rules:
    get: { $ref: "#/components/pathItems/ListTriggerRules" }
    post:
      operationId: createTriggerRule
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TriggerRuleInput" }
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IDResource" }
        default: { $ref: "#/components/responses/Problem" }
  /work-items:
    get: { $ref: "#/components/pathItems/ListWorkItems" }
  /threads:
    get: { $ref: "#/components/pathItems/ListThreads" }
  /jobs:
    get: { $ref: "#/components/pathItems/ListJobs" }
  /workers:
    get: { $ref: "#/components/pathItems/ListWorkers" }
  /execution-nodes:
    get:
      operationId: listExecutionNodes
      tags: [Execution Nodes]
      responses:
        "200":
          description: 执行节点列表
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties: { items: { type: array, items: { $ref: "#/components/schemas/ExecutionNode" } } }
    post:
      operationId: createExecutionNode
      tags: [Execution Nodes]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/ExecutionNodeInput" } } }
      responses:
        "201":
          description: 节点与一次性注册 Token
          content: { application/json: { schema: { $ref: "#/components/schemas/ExecutionNodeCreated" } } }
  /execution-nodes/{id}/enrollments:
    post:
      operationId: createExecutionNodeEnrollment
      tags: [Execution Nodes]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "201": { description: 一次性注册 Token, content: { application/json: { schema: { $ref: "#/components/schemas/EnrollmentToken" } } } }
  /execution-nodes/{id}:
    delete:
      operationId: deleteExecutionNode
      tags: [Execution Nodes]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses: { "204": { description: 已删除 }, default: { $ref: "#/components/responses/Problem" } }
  /execution-nodes/{id}/enabled:
    put:
      operationId: setExecutionNodeEnabled
      tags: [Execution Nodes]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [enabled], properties: { enabled: { type: boolean } } } } } }
      responses: { "204": { description: 已更新 }, default: { $ref: "#/components/responses/Problem" } }
  /settings/execution:
    get:
      operationId: getExecutionDefaults
      tags: [Execution Nodes]
      responses: { "200": { description: 默认节点, content: { application/json: { schema: { $ref: "#/components/schemas/ExecutionDefaults" } } } } }
    put:
      operationId: putExecutionDefaults
      tags: [Execution Nodes]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ExecutionDefaults" } } } }
      responses: { "204": { description: 已保存 }, default: { $ref: "#/components/responses/Problem" } }
  /ssh/credentials:
    get:
      operationId: listSSHCredentials
      tags: [SSH]
      responses:
        "200":
          description: SSH 凭证列表，不包含私钥或口令
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredentialList" } } }
        default: { $ref: "#/components/responses/Problem" }
    post:
      operationId: createSSHCredential
      tags: [SSH]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredentialCreateInput" } } }
      responses:
        "201":
          description: 已创建的脱敏 SSH 凭证
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredential" } } }
        default: { $ref: "#/components/responses/Problem" }
  /ssh/credentials/{id}:
    put:
      operationId: updateSSHCredential
      tags: [SSH]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredentialUpdateInput" } } }
      responses:
        "200":
          description: 已更新的脱敏 SSH 凭证
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHCredential" } } }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteSSHCredential
      tags: [SSH]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "204": { description: 已删除 }
        default: { $ref: "#/components/responses/Problem" }
  /ssh/hosts:
    get:
      operationId: listSSHHosts
      tags: [SSH]
      responses:
        "200":
          description: SSH 主机列表
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostList" } } }
        default: { $ref: "#/components/responses/Problem" }
    post:
      operationId: createSSHHost
      tags: [SSH]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostInput" } } }
      responses:
        "201":
          description: 已创建的 SSH 主机
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHHost" } } }
        default: { $ref: "#/components/responses/Problem" }
  /ssh/hosts/import:
    post:
      operationId: importSSHHosts
      tags: [SSH]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostImportInput" } } }
      responses:
        "201":
          description: 已批量导入的 SSH 主机
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostList" } } }
        default: { $ref: "#/components/responses/Problem" }
  /ssh/hosts/{id}:
    put:
      operationId: updateSSHHost
      tags: [SSH]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SSHHostInput" } } }
      responses:
        "200":
          description: 已更新的 SSH 主机
          content: { application/json: { schema: { $ref: "#/components/schemas/SSHHost" } } }
        default: { $ref: "#/components/responses/Problem" }
    delete:
      operationId: deleteSSHHost
      tags: [SSH]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { $ref: "#/components/parameters/CSRFToken" }
      responses:
        "204": { description: 已删除 }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/ssh-configuration:
    get:
      operationId: getWorkerSSHConfiguration
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters:
        - { name: If-None-Match, in: header, required: false, schema: { type: string } }
      responses:
        "200":
          description: 当前执行节点的 SSH 配置
          headers:
            ETag: { schema: { type: string } }
            Cache-Control: { schema: { type: string, enum: [no-store] } }
          content: { application/json: { schema: { $ref: "#/components/schemas/WorkerSSHConfiguration" } } }
        "304": { description: 配置未变化 }
        default: { $ref: "#/components/responses/Problem" }
  /worker/v1/enroll:
    post:
      operationId: enrollWorker
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: []
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerEnrollRequest" } } } }
      responses: { "200": { description: 长期节点凭据, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerEnrollResponse" } } } }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/heartbeat:
    post:
      operationId: heartbeatWorker
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerHeartbeat" } } } }
      responses: { "204": { description: 心跳已记录 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/claims:
    post:
      operationId: claimWorkerTask
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerClaimRequest" } } } }
      responses: { "200": { description: 任务、开发环境 Operation 或空响应, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerClaimResponse" } } } }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/heartbeat:
    post:
      operationId: heartbeatWorkerRun
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerRunLease" } } } }
      responses: { "200": { description: Lease 已续期及待执行命令, content: { application/json: { schema: { type: object, additionalProperties: true } } } }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/commands/ack:
    post:
      operationId: acknowledgeWorkerRunCommand
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerCommandAck" } } } }
      responses: { "204": { description: 命令已确认 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/events:
    post:
      operationId: appendWorkerRunEvents
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerEventsRequest" } } } }
      responses: { "204": { description: 事件已持久化 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/complete:
    post:
      operationId: completeWorkerRun
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerCompleteRequest" } } } }
      responses: { "204": { description: Run 已完成 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/fail:
    post:
      operationId: failWorkerRun
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerFailRequest" } } } }
      responses: { "204": { description: Run 已失败 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/runtime-credential:
    post:
      operationId: getWorkerRuntimeCredential
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerRunLease" } } } }
      responses: { "200": { description: Run 限定运行凭据, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerRuntimeCredential" } } } }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/thread:
    post:
      operationId: setWorkerRunThread
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "204": { description: Thread 已记录 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/submission:
    post:
      operationId: recordWorkerRunSubmission
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "204": { description: Submission 已记录 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/confirm:
    post:
      operationId: confirmWorkerRunTurn
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "204": { description: Turn 已确认 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/development-state:
    post:
      operationId: updateWorkerDevelopmentState
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "204": { description: 开发环境状态已更新 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/workspace-state:
    post:
      operationId: updateWorkerWorkspaceState
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "204": { description: Worktree 状态已更新 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/tools/call:
    post:
      operationId: callWorkerDynamicTool
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "200": { description: Tool 调用结果, content: { application/json: { schema: { type: object, additionalProperties: true } } } }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/git-credential:
    post:
      operationId: getWorkerGitCredential
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
      responses: { "200": { description: Git 凭据, content: { application/json: { schema: { type: object, additionalProperties: true } } } }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/runs/{id}/attachments/{attachmentId}:
    get:
      operationId: downloadWorkerAttachment
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters:
        - { $ref: "#/components/parameters/WorkerResourceID" }
        - { name: attachmentId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: X-Run-Lease-Token, in: header, required: true, schema: { type: string } }
        - { name: X-Run-Lease-Epoch, in: header, required: true, schema: { type: integer, format: int64 } }
      responses: { "200": { description: Discord 附件, content: { application/octet-stream: { schema: { type: string, format: binary } } } }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/development-operations/{id}/heartbeat:
    post:
      operationId: heartbeatWorkerDevelopmentOperation
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerRunLease" } } } }
      responses: { "204": { description: Lease 已续期 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/development-operations/{id}/complete:
    post:
      operationId: completeWorkerDevelopmentOperation
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerOperationTerminal" } } } }
      responses: { "204": { description: Operation 已完成 }, default: { $ref: "#/components/responses/Problem" } }
  /worker/v1/development-operations/{id}/fail:
    post:
      operationId: failWorkerDevelopmentOperation
      tags: [Worker Protocol]
      servers: [{ url: / }]
      security: [{ nodeBearer: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkerResourceID" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WorkerOperationTerminal" } } } }
      responses: { "204": { description: Operation 已失败 }, default: { $ref: "#/components/responses/Problem" } }
  /worktrees:
    get: { $ref: "#/components/pathItems/ListWorktrees" }
  /audit-logs:
    get: { $ref: "#/components/pathItems/ListAuditLogs" }
  /installations:
    get: { $ref: "#/components/pathItems/ListInstallations" }
  /repo-caches:
    get: { $ref: "#/components/pathItems/ListRepoCaches" }
  /system/status:
    get:
      operationId: getSystemStatus
      tags: [Resources]
      responses:
        "200":
          description: 系统状态
          content:
            application/json:
              schema: { type: object, additionalProperties: { type: integer, format: int64 } }
        default: { $ref: "#/components/responses/Problem" }
  /client/auth/login:
    post:
      operationId: clientLogin
      tags: [Client Protocol]
      security: []
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: "#/components/schemas/LoginRequest" } }
      responses:
        "200": { description: 客户端临时登录凭证, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/device-pairings/{id}/claim:
    post:
      operationId: clientClaimDevicePairing
      tags: [Client Protocol]
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [pairingSecret, deviceId, name, platform, credentialHash]
              properties:
                pairingSecret: { type: string }
                deviceId: { type: string, format: uuid }
                name: { type: string, maxLength: 120 }
                platform: { type: string }
                credentialHash: { type: string, minLength: 64, maxLength: 64 }
      responses:
        "200": { description: 等待管理员确认, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/device-pairings/{id}/status:
    get:
      operationId: clientDevicePairingStatus
      tags: [Client Protocol]
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: Authorization, in: header, required: true, schema: { type: string, pattern: '^Pairing ' } }
      responses:
        "200": { description: 设备绑定状态, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/bootstrap:
    get:
      operationId: clientBootstrap
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      responses:
        "200": { description: 协议 v2 启动快照, content: { application/json: { schema: { $ref: "#/components/schemas/ClientBootstrap" } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/sessions:
    get:
      operationId: clientListSessions
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: cursor, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100 } }
        - { name: projectId, in: query, schema: { type: string, format: uuid } }
        - { name: lifecycle, in: query, schema: { type: string, enum: [active, archive_pending, archived, unarchive_pending] } }
      responses:
        "200": { description: 会话分页, content: { application/json: { schema: { type: object, required: [sessions, nextCursor], properties: { sessions: { type: array, items: { $ref: "#/components/schemas/ClientSession" } }, nextCursor: { type: string } } } } } }
        default: { $ref: "#/components/responses/Problem" }
    post:
      operationId: clientCreateSession
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: "#/components/schemas/ClientCreateSession" } }
      responses:
        "201": { description: 原子创建会话、首条消息与 Turn, content: { application/json: { schema: { type: object, required: [session, deduplicated], properties: { session: { $ref: "#/components/schemas/ClientSession" }, deduplicated: { type: boolean } } } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/sessions/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      operationId: clientGetSession
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      responses:
        "200": { description: 会话、目标参数和当前 Run, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
    patch:
      operationId: clientPatchSession
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { type: object, additionalProperties: true } }
      responses:
        "200": { description: 更新后的会话, content: { application/json: { schema: { $ref: "#/components/schemas/ClientSession" } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/sessions/{id}/messages:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      operationId: clientListMessages
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: beforeSeq, in: query, schema: { type: integer, format: int64 } }
        - { name: afterSeq, in: query, schema: { type: integer, format: int64 } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 200 } }
      responses:
        "200": { description: 双向消息分页, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
    post:
      operationId: clientCreateMessage
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { type: object, required: [localId, text], properties: { localId: { type: string }, text: { type: string }, behavior: { type: string, enum: [start_when_idle, steer_if_active] }, attachmentIds: { type: array, maxItems: 10, items: { type: string, format: uuid } } } } }
      responses:
        "201": { description: 已发送或 steer, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/sessions/{id}/stop:
    post:
      operationId: clientStopSession
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "202": { description: 操作已接受, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/sessions/{id}/archive:
    post:
      operationId: clientArchiveSession
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "202": { description: 归档请求已接受, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/sessions/{id}/restore:
    post:
      operationId: clientRestoreSession
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "202": { description: 恢复请求已接受, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/sessions/{id}/plans/{runId}/execute:
    post:
      operationId: clientExecutePlan
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: runId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "201": { description: Plan 执行 Intent, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/uploads:
    post:
      operationId: clientUpload
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [localId, file]
              properties:
                localId: { type: string }
                file: { type: string, format: binary }
      responses:
        "201": { description: 已上传附件, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/attachments/{id}/content:
    get:
      operationId: clientDownloadAttachment
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: 附件内容, content: { application/octet-stream: { schema: { type: string, format: binary } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/interactive/{id}/answer:
    post:
      operationId: clientAnswerInteractive
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json: { schema: { type: object, required: [answer], properties: { answer: { type: object, additionalProperties: true } } } }
      responses:
        "200": { description: 交互回答状态, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/sync:
    get:
      operationId: clientSync
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: afterCursor, in: query, schema: { type: integer, format: int64, minimum: 0 } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 500 } }
      responses:
        "200": { description: 持久同步事件, content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        "410": { description: 游标过期，必须按 Control 重置, content: { application/problem+json: { schema: { $ref: "#/components/schemas/ProblemDetails" } } } }
        default: { $ref: "#/components/responses/Problem" }
  /client/updates:
    get:
      operationId: clientUpdatesWebSocket
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      description: 升级为 WebSocket；durable 事件使用全局 cursor，live 事件使用 runEventSeq。
      parameters:
        - { name: cursor, in: query, schema: { type: integer, format: int64, minimum: 0 } }
      responses:
        "101": { description: WebSocket 已升级 }
        default: { $ref: "#/components/responses/Problem" }
  /client/device/push-token:
    put:
      operationId: clientPutPushToken
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { type: object, required: [token, platform, appEnvironment], properties: { token: { type: string }, platform: { type: string, enum: [ios, android] }, appEnvironment: { type: string, enum: [development, preview, production] } } } }
      responses: { "204": { description: 已注册 }, default: { $ref: "#/components/responses/Problem" } }
    delete:
      operationId: clientDeletePushToken
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      parameters:
        - { name: token, in: query, required: true, schema: { type: string } }
      responses: { "204": { description: 已删除 }, default: { $ref: "#/components/responses/Problem" } }
  /client/device:
    delete:
      operationId: clientDeleteDevice
      tags: [Client Protocol]
      security: [{ clientBearer: [] }]
      responses: { "204": { description: 当前设备已自撤销 }, default: { $ref: "#/components/responses/Problem" } }
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: tyrs_hand_session
    nodeBearer:
      type: http
      scheme: bearer
      bearerFormat: opaque-node-token
    clientBearer:
      type: http
      scheme: bearer
      bearerFormat: tyrs-hand-device-token
  parameters:
    CSRFToken:
      name: X-CSRF-Token
      in: header
      required: true
      schema: { type: string }
    Cursor:
      name: cursor
      in: query
      required: false
      schema: { type: string }
    Limit:
      name: limit
      in: query
      required: false
      schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
    WorkerResourceID:
      name: id
      in: path
      required: true
      schema: { type: string, format: uuid }
  responses:
    Problem:
      description: RFC 9457 Problem Details
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    ResourceList:
      description: 游标分页资源列表
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ResourceList" }
  pathItems:
    ListRepositories:
      operationId: listRepositories
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListAgentProfiles:
      operationId: listAgentProfiles
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListTriggerRules:
      operationId: listTriggerRules
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListWorkItems:
      operationId: listWorkItems
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListThreads:
      operationId: listThreads
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListJobs:
      operationId: listJobs
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListWorkers:
      operationId: listWorkers
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListWorktrees:
      operationId: listWorktrees
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListAuditLogs:
      operationId: listAuditLogs
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListInstallations:
      operationId: listInstallations
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    ListRepoCaches:
      operationId: listRepoCaches
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { $ref: "#/components/responses/ResourceList" }, default: { $ref: "#/components/responses/Problem" } }
    CreateGeneric:
      tags: [Resources]
      parameters: [{ $ref: "#/components/parameters/CSRFToken" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/IDResource" }
        default: { $ref: "#/components/responses/Problem" }
  schemas:
    ClientSessionSettings:
      type: object
      required: [agentProfileId, model, reasoningEffort, serviceTier, collaborationMode, settingsVersion]
      properties:
        agentProfileId: { type: string, format: uuid }
        model: { type: string, nullable: true }
        reasoningEffort: { type: string, nullable: true, enum: [low, medium, high, xhigh, max, ultra] }
        serviceTier: { type: string, enum: [standard, fast] }
        collaborationMode: { type: string, enum: [default, plan] }
        settingsVersion: { type: integer, format: int64, minimum: 0 }
    ClientSession:
      type: object
      required: [id, developmentEnvironmentId, developmentProjectId, agentProfileId, title,
        lifecycleState, historyCompleteness, model, reasoningEffort, serviceTier,
        collaborationMode, settingsVersion, lastMessageSeq, lastActivityAt, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        developmentEnvironmentId: { type: string, format: uuid }
        developmentProjectId: { type: string, format: uuid }
        agentProfileId: { type: string, format: uuid }
        title: { type: string }
        lifecycleState: { type: string, enum: [active, archive_pending, archived, unarchive_pending] }
        historyCompleteness: { type: string, enum: [complete, partial] }
        model: { type: string, nullable: true }
        reasoningEffort: { type: string, nullable: true }
        serviceTier: { type: string, enum: [standard, fast] }
        collaborationMode: { type: string, enum: [default, plan] }
        settingsVersion: { type: integer, format: int64 }
        lastMessageSeq: { type: integer, format: int64 }
        lastActivityAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ClientCreateSession:
      type: object
      required: [projectId, settings, initialMessage]
      properties:
        projectId: { type: string, format: uuid }
        settings: { $ref: "#/components/schemas/ClientSessionSettings" }
        initialMessage:
          type: object
          required: [localId, text]
          properties:
            localId: { type: string, minLength: 1, maxLength: 200 }
            text: { type: string, minLength: 1 }
            attachmentIds: { type: array, maxItems: 10, items: { type: string, format: uuid } }
    ClientProject:
      type: object
      required: [id, environmentId, name, relativePath, kind, availabilityStatus, branch, dirty]
      properties:
        id: { type: string, format: uuid }
        environmentId: { type: string, format: uuid }
        name: { type: string }
        relativePath: { type: string }
        kind: { type: string }
        availabilityStatus: { type: string }
        branch: { type: string, nullable: true }
        dirty: { type: boolean }
    ClientModel:
      type: object
      required: [id, displayName, description, supportedReasoningEfforts,
        defaultReasoningEffort, inputModalities, serviceTiers, defaultServiceTier, default]
      properties:
        id: { type: string }
        displayName: { type: string }
        description: { type: string }
        supportedReasoningEfforts:
          type: array
          items:
            type: object
            required: [id, description]
            properties:
              id: { type: string, enum: [low, medium, high, xhigh, max, ultra] }
              description: { type: string }
        defaultReasoningEffort: { type: string, enum: [low, medium, high, xhigh, max, ultra] }
        inputModalities: { type: array, items: { type: string, enum: [text, image] } }
        serviceTiers:
          type: array
          items:
            type: object
            required: [id, name, description]
            properties:
              id: { type: string, enum: [fast] }
              name: { type: string }
              description: { type: string }
        defaultServiceTier: { type: string, enum: [standard, fast] }
        default: { type: boolean }
    ClientBootstrap:
      type: object
      required: [serverId, protocolVersion, currentCursor, user, capabilities, environments,
        projects, agentProfiles, modelCatalog, lastStartedSettings]
      properties:
        serverId: { type: string, format: uuid }
        protocolVersion: { type: integer, enum: [2] }
        currentCursor: { type: integer, format: int64, minimum: 0 }
        user:
          type: object
          required: [id, username]
          properties:
            id: { type: string, format: uuid }
            username: { type: string }
        capabilities: { type: object, additionalProperties: { type: boolean } }
        environments: { type: array, items: { type: object, additionalProperties: true } }
        projects: { type: array, items: { $ref: "#/components/schemas/ClientProject" } }
        agentProfiles:
          type: array
          items:
            type: object
            required: [id, name]
            properties:
              id: { type: string, format: uuid }
              name: { type: string }
        modelCatalog: { type: array, items: { $ref: "#/components/schemas/ClientModel" } }
        lastStartedSettings:
          allOf: [{ $ref: "#/components/schemas/ClientSessionSettings" }]
          nullable: true
    ExecutionNode:
      type: object
      required: [id, name, roles, enabled, maxConcurrentJobs, protocolVersion, status, metadata]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        roles: { type: array, items: { type: string, enum: [github, discord] } }
        enabled: { type: boolean }
        maxConcurrentJobs: { type: integer, minimum: 1 }
        protocolVersion: { type: integer }
        workerVersion: { type: string }
        status: { type: string, enum: [pending, online, offline, disabled, incompatible, error] }
        heartbeatAt: { type: string, format: date-time }
        lastError: { type: string }
        metadata: { type: object, additionalProperties: true }
    ExecutionNodeInput:
      type: object
      required: [name, roles, maxConcurrentJobs]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        roles: { type: array, minItems: 1, items: { type: string, enum: [github, discord] } }
        maxConcurrentJobs: { type: integer, minimum: 1 }
    EnrollmentToken:
      type: object
      required: [enrollmentToken, expiresIn]
      properties:
        enrollmentToken: { type: string, writeOnly: true }
        expiresIn: { type: integer, enum: [900] }
    ExecutionNodeCreated:
      allOf:
        - { $ref: "#/components/schemas/EnrollmentToken" }
        - type: object
          required: [node]
          properties: { node: { $ref: "#/components/schemas/ExecutionNode" } }
    ExecutionDefaults:
      type: object
      properties:
        githubNodeId: { type: string, format: uuid, nullable: true }
        discordNodeId: { type: string, format: uuid, nullable: true }
    SSHCredential:
      type: object
      required: [id, name, publicKey, fingerprint, enabled, version, hostCount, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        publicKey: { type: string, readOnly: true }
        fingerprint: { type: string, readOnly: true }
        enabled: { type: boolean }
        version: { type: integer, format: int64, readOnly: true }
        hostCount: { type: integer, readOnly: true }
        updatedAt: { type: string, format: date-time, readOnly: true }
    SSHCredentialList:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/SSHCredential" } }
    SSHCredentialCreateInput:
      type: object
      required: [name, privateKey, enabled]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        privateKey: { type: string, minLength: 1, writeOnly: true }
        passphrase: { type: string, writeOnly: true }
        enabled: { type: boolean }
    SSHCredentialUpdateInput:
      type: object
      required: [name, enabled]
      properties:
        name: { type: string, minLength: 1, maxLength: 128 }
        privateKey:
          type: string
          writeOnly: true
          description: 非空时显式轮换私钥；空字符串表示保持现有私钥
        passphrase: { type: string, writeOnly: true }
        enabled: { type: boolean }
    SSHHost:
      type: object
      required: [id, alias, hostname, port, username, credentialId, credentialName, executionNodeIds, enabled, updatedAt]
      properties:
        id: { type: string, format: uuid }
        alias: { type: string }
        hostname: { type: string }
        port: { type: integer, minimum: 1, maximum: 65535 }
        username: { type: string }
        credentialId: { type: string, format: uuid }
        credentialName: { type: string, readOnly: true }
        proxyJumpHostId: { type: string, format: uuid }
        proxyJumpAlias: { type: string, readOnly: true }
        executionNodeIds: { type: array, items: { type: string, format: uuid } }
        enabled: { type: boolean }
        updatedAt: { type: string, format: date-time, readOnly: true }
    SSHHostInput:
      type: object
      required: [alias, hostname, port, username, credentialId, executionNodeIds, enabled]
      properties:
        alias: { type: string, minLength: 1, maxLength: 128, pattern: "^[A-Za-z0-9._-]+$" }
        hostname: { type: string, minLength: 1, maxLength: 255 }
        port: { type: integer, minimum: 1, maximum: 65535 }
        username: { type: string, minLength: 1, maxLength: 128 }
        credentialId: { type: string, format: uuid }
        proxyJumpHostId: { type: string, format: uuid }
        executionNodeIds: { type: array, items: { type: string, format: uuid }, uniqueItems: true }
        enabled: { type: boolean }
    SSHHostImportItem:
      type: object
      required: [alias, hostname, port, username]
      properties:
        alias: { type: string, minLength: 1, maxLength: 128, pattern: "^[A-Za-z0-9._-]+$" }
        hostname: { type: string, minLength: 1, maxLength: 255 }
        port: { type: integer, minimum: 1, maximum: 65535 }
        username: { type: string, minLength: 1, maxLength: 128 }
        proxyJumpAlias: { type: string, minLength: 1, maxLength: 128, pattern: "^[A-Za-z0-9._-]+$" }
    SSHHostImportInput:
      type: object
      required: [credentialId, executionNodeIds, enabled, hosts]
      properties:
        credentialId: { type: string, format: uuid }
        executionNodeIds: { type: array, items: { type: string, format: uuid }, uniqueItems: true }
        enabled: { type: boolean }
        hosts:
          type: array
          minItems: 1
          maxItems: 100
          items: { $ref: "#/components/schemas/SSHHostImportItem" }
    SSHHostList:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/SSHHost" } }
    WorkerSSHCredential:
      type: object
      required: [id, privateKey, publicKey, fingerprint]
      properties:
        id: { type: string, format: uuid }
        privateKey: { type: string, writeOnly: true }
        passphrase: { type: string, writeOnly: true }
        publicKey: { type: string }
        fingerprint: { type: string }
    WorkerSSHHost:
      type: object
      required: [alias, hostname, port, username, credentialId]
      properties:
        alias: { type: string }
        hostname: { type: string }
        port: { type: integer, minimum: 1, maximum: 65535 }
        username: { type: string }
        credentialId: { type: string, format: uuid }
        proxyJumpAlias: { type: string }
    WorkerSSHConfiguration:
      type: object
      required: [revision, credentials, hosts]
      properties:
        revision: { type: string }
        credentials: { type: array, items: { $ref: "#/components/schemas/WorkerSSHCredential" } }
        hosts: { type: array, items: { $ref: "#/components/schemas/WorkerSSHHost" } }
    WorkerEnrollRequest:
      type: object
      required: [token]
      properties: { token: { type: string, writeOnly: true } }
    WorkerEnrollResponse:
      type: object
      required: [nodeId, credential, protocolVersion]
      properties:
        nodeId: { type: string, format: uuid }
        credential: { type: string, writeOnly: true }
        protocolVersion: { type: integer }
    WorkerHeartbeat:
      type: object
      required: [workerVersion, protocolVersion]
      properties:
        workerVersion: { type: string }
        protocolVersion: { type: integer }
        metadata: { type: object, additionalProperties: true }
    WorkerClaimRequest:
      type: object
      required: [workerId, role, wait]
      properties:
        workerId: { type: string }
        role: { type: string, enum: [github, discord] }
        wait: { type: boolean }
    WorkerClaimResponse:
      type: object
      properties:
        task: { type: object, additionalProperties: true }
        developmentOperation: { $ref: "#/components/schemas/WorkerDevelopmentOperation" }
    WorkerDevelopmentOperation:
      type: object
      required: [id, operation, leaseToken, leaseEpoch, environmentId, containerName, dataVolume, homeVolume, network]
      properties:
        id: { type: string, format: uuid }
        operation: { type: string, enum: [provision, clone, rebase, reconfigure, delete_forum, delete_environment] }
        leaseToken: { type: string, writeOnly: true }
        leaseEpoch: { type: integer, format: int64 }
        environmentId: { type: string, format: uuid }
        forumId: { type: string, format: uuid }
        containerName: { type: string }
        imageRef: { type: string }
        dataVolume: { type: string }
        homeVolume: { type: string }
        network: { type: string }
        workspace: { type: string }
        conversationIds: { type: array, items: { type: string, format: uuid } }
        runtimeUser: { type: string }
        runtimeUid: { type: integer, format: int64 }
        runtimeGid: { type: integer, format: int64 }
        runtimeHome: { type: string }
        sshPublicKey: { type: string }
        sshPort: { type: integer, minimum: 1, maximum: 65535 }
        sshConfigRevision: { type: integer, format: int64 }
    WorkerRunLease:
      type: object
      required: [leaseToken, leaseEpoch]
      properties:
        leaseToken: { type: string, writeOnly: true }
        leaseEpoch: { type: integer, format: int64 }
    WorkerEvent:
      type: object
      required: [sequence, type, payload]
      properties:
        sequence: { type: integer, format: int64, minimum: 1 }
        type: { type: string }
        payload: { type: object, additionalProperties: true }
    WorkerEventsRequest:
      allOf:
        - { $ref: "#/components/schemas/WorkerRunLease" }
        - type: object
          required: [events]
          properties:
            events: { type: array, items: { $ref: "#/components/schemas/WorkerEvent" } }
    WorkerCompleteRequest:
      allOf:
        - { $ref: "#/components/schemas/WorkerRunLease" }
        - type: object
          required: [idempotencyKey, result]
          properties:
            idempotencyKey: { type: string }
            result: { type: object, additionalProperties: true }
    WorkerFailRequest:
      allOf:
        - { $ref: "#/components/schemas/WorkerRunLease" }
        - type: object
          required: [idempotencyKey, code, message]
          properties:
            idempotencyKey: { type: string }
            code: { type: string }
            message: { type: string }
    WorkerCommandAck:
      allOf:
        - { $ref: "#/components/schemas/WorkerRunLease" }
        - type: object
          required: [commandId, action]
          properties:
            commandId: { type: string, format: uuid }
            action: { type: string, enum: [steer, interrupt] }
            turnId: { type: string }
    WorkerOperationTerminal:
      allOf:
        - { $ref: "#/components/schemas/WorkerRunLease" }
        - type: object
          required: [idempotencyKey]
          properties:
            idempotencyKey: { type: string }
            error: { type: string }
            appliedRevision: { type: integer, format: int64 }
            containerId: { type: string }
            imageRef: { type: string }
            imageId: { type: string }
            daemonStatus: { type: string }
    WorkerRuntimeCredential:
      type: object
      required: [modelSource, chatgptAuthRevision]
      properties:
        apiKey: { type: string, writeOnly: true }
        baseUrl: { type: string }
        proxyUrl: { type: string }
        modelSource: { type: string, enum: [chatgpt, provider] }
        chatgptAuth:
          type: object
          additionalProperties: true
          writeOnly: true
        chatgptAuthRevision: { type: integer, format: int64 }
        configSignature: { type: string }
        globalAgents: { type: string }
    ProblemDetails:
      type: object
      required: [type, title, status]
      properties:
        type: { type: string, format: uri-reference }
        title: { type: string }
        status: { type: integer, minimum: 400, maximum: 599 }
        detail: { type: string }
        instance: { type: string, format: uri-reference }
        requestId: { type: string }
    SetupStatus:
      type: object
      required: [setupRequired, githubConfigured]
      properties:
        setupRequired: { type: boolean }
        githubConfigured: { type: boolean }
    SetupRequest:
      type: object
      required: [setupToken, username, password]
      properties:
        setupToken: { type: string, minLength: 16 }
        username: { type: string, minLength: 3, maxLength: 64 }
        password: { type: string, minLength: 12, maxLength: 256 }
    SetupResult:
      type: object
      required: [totpSecret, provisioningUri, recoveryCodes]
      properties:
        totpSecret: { type: string }
        provisioningUri: { type: string, format: uri }
        recoveryCodes: { type: array, items: { type: string } }
    LoginRequest:
      type: object
      required: [username, password, totp]
      properties:
        username: { type: string }
        password: { type: string }
        totp: { type: string, pattern: "^[0-9]{6}$" }
    LoginResult:
      type: object
      required: [username, csrfToken, expiresAt]
      properties:
        username: { type: string }
        csrfToken: { type: string }
        expiresAt: { type: string, format: date-time }
    Administrator:
      type: object
      required: [id, username, csrfToken, expiresAt]
      properties:
        id: { type: string, format: uuid }
        username: { type: string }
        csrfToken: { type: string }
        expiresAt: { type: string, format: date-time }
    GitHubAppStatus:
      type: object
      required: [configured]
      properties:
        configured: { type: boolean }
        appId: { type: integer, format: int64 }
        clientId: { type: string }
        appSlug: { type: string }
    GitHubAppInput:
      type: object
      required: [appId, appSlug, privateKey, webhookSecret]
      properties:
        appId: { type: integer, format: int64, minimum: 1 }
        clientId: { type: string }
        appSlug: { type: string }
        privateKey: { type: string, writeOnly: true }
        webhookSecret: { type: string, writeOnly: true }
        clientSecret: { type: string, writeOnly: true }
    RepositoryInput:
      type: object
      required: [installationExternalId, accountLogin, repositoryExternalId, owner, name, defaultBranch, cloneUrl]
      properties:
        installationExternalId: { type: integer, format: int64 }
        accountLogin: { type: string }
        accountType: { type: string, default: Organization }
        repositoryExternalId: { type: integer, format: int64 }
        owner: { type: string }
        name: { type: string }
        defaultBranch: { type: string }
        cloneUrl: { type: string, format: uri }
    TriggerRuleInput:
      type: object
      required: [repositoryId, agentProfileId, name, eventName, triggerKind, instruction]
      properties:
        repositoryId: { type: string, format: uuid }
        agentProfileId: { type: string, format: uuid }
        name: { type: string, minLength: 1, maxLength: 128 }
        eventName: { type: string, minLength: 1 }
        action: { type: string }
        enabled: { type: boolean }
        priority: { type: integer, default: 100 }
        triggerKind:
          type: string
          enum: [event, label, slash_command, mention_command, legacy_mention]
        triggerValue:
          type: string
          description: Label 名或不带斜杠的命令名；event、mention_command 与 legacy_mention 留空。
        actorMinPermission:
          type: string
          enum: [read, triage, write, maintain, admin]
          default: triage
        instruction: { type: string, minLength: 1 }
        skills: { type: array, items: { type: string } }
        allowedTools: { type: array, items: { type: string } }
        dangerousActions: { type: array, items: { type: string } }
        filters: { type: object, additionalProperties: true }
    AgentProviderSettings:
      type: object
      required:
        - modelSource
        - providerConfigured
        - chatgptConfigured
        - chatgptAuthRevision
        - configSignature
        - chatgptAccount
      properties:
        modelSource: { type: string, enum: [chatgpt, provider] }
        baseUrl: { type: string }
        model: { type: string }
        reasoningEffort: { type: string }
        serviceTier: { type: string }
        proxyUrl: { type: string }
        providerConfigured: { type: boolean, readOnly: true }
        chatgptConfigured: { type: boolean, readOnly: true }
        chatgptAuthRevision: { type: integer, format: int64, readOnly: true }
        configSignature: { type: string, readOnly: true }
        chatgptAccount: { $ref: "#/components/schemas/ChatGPTAccount" }
    AgentProviderSettingsInput:
      type: object
      required: [modelSource]
      properties:
        modelSource: { type: string, enum: [chatgpt, provider] }
        baseUrl: { type: string }
        apiKey: { type: string, writeOnly: true }
        model: { type: string }
        reasoningEffort: { type: string }
        serviceTier: { type: string }
        proxyUrl: { type: string }
    ChatGPTAccount:
      type: object
      required: [configured]
      properties:
        configured: { type: boolean }
        email: { type: string }
        planType: { type: string }
    ChatGPTLoginOperation:
      type: object
      required: [id, status]
      properties:
        id: { type: string, format: uuid }
        status:
          type: string
          enum: [pending, awaiting_user, completed, failed, canceled]
        verificationUrl: { type: string, format: uri }
        userCode: { type: string }
        email: { type: string }
        planType: { type: string }
        error: { type: string }
        expiresAt: { type: string, format: date-time }
    GlobalAgents:
      type: object
      required: [content]
      properties:
        content: { type: string, maxLength: 262144 }
    CodexPreferences:
      type: object
      properties:
        model: { type: string, nullable: true, maxLength: 128 }
        reasoningEffort: { type: string, nullable: true, enum: [low, medium, high, xhigh] }
        serviceTier: { type: string, nullable: true, enum: [standard, fast] }
    CodexEffectivePreferences:
      type: object
      required: [model, reasoningEffort, serviceTier]
      properties:
        model: { type: string }
        reasoningEffort: { type: string }
        serviceTier: { type: string, enum: [standard, fast] }
    CodexForumSettings:
      type: object
      required: [id, name, ownerDiscordUserId, settings, effective]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        ownerDiscordUserId: { type: string }
        settings: { $ref: "#/components/schemas/CodexPreferences" }
        effective: { $ref: "#/components/schemas/CodexEffectivePreferences" }
    CodexRepositorySettings:
      type: object
      required: [id, owner, name, settings, effective, forums]
      properties:
        id: { type: string, format: uuid }
        owner: { type: string }
        name: { type: string }
        settings: { $ref: "#/components/schemas/CodexPreferences" }
        effective: { $ref: "#/components/schemas/CodexEffectivePreferences" }
        forums: { type: array, items: { $ref: "#/components/schemas/CodexForumSettings" } }
    CodexSettingsList:
      type: object
      required: [items, modelOptions]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/CodexRepositorySettings" } }
        modelOptions: { type: array, items: { type: string } }
    DiscordSettings:
      type: object
      required: [guildId, enabled, communityEnabled, tokenConfigured]
      properties:
        guildId: { type: string }
        enabled: { type: boolean }
        communityEnabled: { type: boolean }
        applicationId: { type: string }
        botUserId: { type: string }
        tokenConfigured: { type: boolean }
    DiscordSettingsInput:
      type: object
      required: [guildId, enabled]
      properties:
        guildId: { type: string, pattern: "^[0-9]+$" }
        enabled: { type: boolean }
        botToken: { type: string, writeOnly: true }
        applicationId: { type: string, pattern: "^[0-9]*$" }
        botUserId: { type: string, pattern: "^[0-9]*$" }
    DiscordStatus:
      type: object
      required: [configured, enabled, gatewayStatus, pendingOutbox, failedOutbox, pendingInitializationOperations]
      properties:
        configured: { type: boolean }
        enabled: { type: boolean }
        gatewayStatus: { type: string }
        gatewayError: { type: string }
        lastGatewayAt: { type: string, format: date-time }
        pendingOutbox: { type: integer, format: int64 }
        failedOutbox: { type: integer, format: int64 }
        pendingInitializationOperations: { type: integer, format: int64 }
    DiscordInitializationInput:
      type: object
      required: [mode]
      properties:
        mode: { type: string, enum: [incremental, fresh] }
        confirmation: { type: string }
    DiscordConflict:
      type: object
      required: [name, reason]
      properties:
        name: { type: string }
        reason: { type: string }
    DiscordPreflight:
      type: object
      required: [guildId, mode, creates, updates, deletes, conflicts, missingPermissions, channelCount, safe]
      properties:
        guildId: { type: string }
        mode: { type: string, enum: [incremental, fresh] }
        creates: { type: array, items: { type: string } }
        updates: { type: array, items: { type: string } }
        deletes: { type: array, items: { type: string } }
        conflicts: { type: array, items: { $ref: "#/components/schemas/DiscordConflict" } }
        missingPermissions: { type: array, items: { type: string } }
        channelCount: { type: integer }
        safe: { type: boolean }
    DiscordInitialization:
      type: object
      required: [id, guildId, mode, status, preflight, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        guildId: { type: string }
        mode: { type: string }
        status: { type: string }
        preflight: { $ref: "#/components/schemas/DiscordPreflight" }
        error: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    DiscordMember:
      type: object
      required: [guildId, discordUserId, username, displayName, bound]
      properties:
        guildId: { type: string }
        discordUserId: { type: string }
        username: { type: string }
        displayName: { type: string }
        bound: { type: boolean }
        githubLogin: { type: string }
    DevelopmentForumCollaborator:
      type: object
      required: [forumId, memberId, accessLevel, administratorBypass]
      properties:
        forumId: { type: string, format: uuid }
        memberId: { type: string }
        accessLevel: { type: string, enum: [readonly, operator] }
        administratorBypass: { type: boolean }
    DevelopmentForum:
      type: object
      required: [id, name, discordId, bindingStatus, collaborators]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        discordId: { type: string }
        bindingStatus: { type: string, enum: [active, inactive] }
        collaborators:
          type: array
          items: { $ref: "#/components/schemas/DevelopmentForumCollaborator" }
    DevelopmentProject:
      type: object
      required: [id, name, relativePath, projectKind, availabilityStatus, dirty, lastSeenAt, forums]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        relativePath: { type: string }
        desiredRelativePath: { type: string }
        projectKind: { type: string, enum: [directory, git] }
        availabilityStatus: { type: string, enum: [available, missing] }
        branch: { type: string }
        headSha: { type: string }
        dirty: { type: boolean }
        remoteUrl: { type: string }
        lastSeenAt: { type: string, format: date-time }
        scanError: { type: string }
        forums:
          type: array
          items: { $ref: "#/components/schemas/DevelopmentForum" }
    DevelopmentEnvironment:
      type: object
      required: [id, ownerDiscordUserId, ownerName, status, imageRef, codexUserOverride, lastUsedAt, sshConfigRevision, sshAppliedRevision, daemonStatus, appServerStatus, sshStatus, relayStatus, projects]
      properties:
        id: { type: string, format: uuid }
        ownerDiscordUserId: { type: string }
        ownerName: { type: string }
        status: { type: string }
        imageRef: { type: string }
        imageId: { type: string }
        runtimeUser: { type: string }
        codexVersion: { type: string }
        codexUserOverride: { type: boolean }
        lastUsedAt: { type: string, format: date-time }
        error: { type: string }
        executionNodeId: { type: string, format: uuid }
        sshPublicKey: { type: string }
        sshFingerprint: { type: string, readOnly: true }
        sshPort: { type: integer, minimum: 1, maximum: 65535 }
        sshDiscordUserId: { type: string }
        sshDisplayName: { type: string, readOnly: true }
        sshConfigRevision: { type: integer, format: int64, readOnly: true }
        sshAppliedRevision: { type: integer, format: int64, readOnly: true }
        daemonStatus: { type: string, enum: [pending, starting, running, error] }
        daemonError: { type: string }
        appServerStatus: { type: string, enum: [pending, starting, running, error] }
        sshStatus: { type: string, enum: [disabled, pending, starting, running, error] }
        relayStatus: { type: string, enum: [pending, starting, running, error] }
        projectsScannedAt: { type: string, format: date-time }
        projectScanError: { type: string }
        projects:
          type: array
          items: { $ref: "#/components/schemas/DevelopmentProject" }
    DevelopmentEnvironmentList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/DevelopmentEnvironment" }
    DevelopmentEnvironmentSSHInput:
      type: object
      required: [publicKey, port, discordUserId]
      properties:
        publicKey: { type: string, minLength: 1 }
        port: { type: integer, minimum: 1, maximum: 65535 }
        discordUserId: { type: string, minLength: 1 }
    DevelopmentEnvironmentOperation:
      type: object
      required: [id, operationId]
      properties:
        id: { type: string, format: uuid }
        operationId: { type: string, format: uuid }
    DevelopmentProjectForumInput:
      type: object
      required: [mode]
      properties:
        mode: { type: string, enum: [new, restore] }
        forumId: { type: string, format: uuid }
        name: { type: string, maxLength: 100 }
    IDResource:
      type: object
      required: [id]
      properties:
        id: { type: string, format: uuid }
    ResourceList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: { type: object, additionalProperties: true }
        nextCursor: { type: string }
security:
  - sessionCookie: []
