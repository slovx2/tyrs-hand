// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/slovx2/tyrs-hand/ent/repocache"
)

// RepoCache is the model entity for the RepoCache schema.
type RepoCache struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// RepositoryID holds the value of the "repository_id" field.
	RepositoryID uuid.UUID `json:"repository_id,omitempty"`
	// ExecutionNodeID holds the value of the "execution_node_id" field.
	ExecutionNodeID *uuid.UUID `json:"execution_node_id,omitempty"`
	// Path holds the value of the "path" field.
	Path string `json:"path,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// SizeBytes holds the value of the "size_bytes" field.
	SizeBytes int64 `json:"size_bytes,omitempty"`
	// LastFetchAt holds the value of the "last_fetch_at" field.
	LastFetchAt *time.Time `json:"last_fetch_at,omitempty"`
	// LastUsedAt holds the value of the "last_used_at" field.
	LastUsedAt time.Time `json:"last_used_at,omitempty"`
	// Error holds the value of the "error" field.
	Error        *string `json:"error,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RepoCache) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case repocache.FieldExecutionNodeID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case repocache.FieldSizeBytes:
			values[i] = new(sql.NullInt64)
		case repocache.FieldPath, repocache.FieldStatus, repocache.FieldError:
			values[i] = new(sql.NullString)
		case repocache.FieldLastFetchAt, repocache.FieldLastUsedAt:
			values[i] = new(sql.NullTime)
		case repocache.FieldID, repocache.FieldRepositoryID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RepoCache fields.
func (_m *RepoCache) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case repocache.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case repocache.FieldRepositoryID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field repository_id", values[i])
			} else if value != nil {
				_m.RepositoryID = *value
			}
		case repocache.FieldExecutionNodeID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field execution_node_id", values[i])
			} else if value.Valid {
				_m.ExecutionNodeID = new(uuid.UUID)
				*_m.ExecutionNodeID = *value.S.(*uuid.UUID)
			}
		case repocache.FieldPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field path", values[i])
			} else if value.Valid {
				_m.Path = value.String
			}
		case repocache.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case repocache.FieldSizeBytes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field size_bytes", values[i])
			} else if value.Valid {
				_m.SizeBytes = value.Int64
			}
		case repocache.FieldLastFetchAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_fetch_at", values[i])
			} else if value.Valid {
				_m.LastFetchAt = new(time.Time)
				*_m.LastFetchAt = value.Time
			}
		case repocache.FieldLastUsedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_used_at", values[i])
			} else if value.Valid {
				_m.LastUsedAt = value.Time
			}
		case repocache.FieldError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error", values[i])
			} else if value.Valid {
				_m.Error = new(string)
				*_m.Error = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RepoCache.
// This includes values selected through modifiers, order, etc.
func (_m *RepoCache) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this RepoCache.
// Note that you need to call RepoCache.Unwrap() before calling this method if this RepoCache
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RepoCache) Update() *RepoCacheUpdateOne {
	return NewRepoCacheClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RepoCache entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RepoCache) Unwrap() *RepoCache {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RepoCache is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RepoCache) String() string {
	var builder strings.Builder
	builder.WriteString("RepoCache(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("repository_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RepositoryID))
	builder.WriteString(", ")
	if v := _m.ExecutionNodeID; v != nil {
		builder.WriteString("execution_node_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("path=")
	builder.WriteString(_m.Path)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("size_bytes=")
	builder.WriteString(fmt.Sprintf("%v", _m.SizeBytes))
	builder.WriteString(", ")
	if v := _m.LastFetchAt; v != nil {
		builder.WriteString("last_fetch_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("last_used_at=")
	builder.WriteString(_m.LastUsedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.Error; v != nil {
		builder.WriteString("error=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// RepoCaches is a parsable slice of RepoCache.
type RepoCaches []*RepoCache
